SpriteSheet(
    texture_width: 32,  // Width of the sprite sheet image, which is the width of the single sprite
    texture_height: 32, // Height of the sprite sheet image, which is the height of the single sprite
    sprites: [
        Sprite {
            x: 0,      // The x coordinate of the top-left corner of the sprite (since it's the only sprite, this is 0)
            y: 0,      // The y coordinate of the top-left corner of the sprite (since it's the only sprite, this is 0)
            width: 32,  // Width of the sprite
            height: 32, // Height of the sprite
        },
    ],
)